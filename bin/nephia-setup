#!/usr/bin/env perl

use strict;
use warnings;
use Nephia::Setup;

my $class = shift @ARGV;
$class ? Nephia::Setup::create( $class ) : help();

sub help {
    print <<EOF;
Usage: 
nephia-setup [AppName]
EOF
}
