#!/usr/bin/env perl

use strict;
use warnings;
use Nephia::Setup;

my( $class, $flavor ) = @ARGV;
$class ? Nephia::Setup::create( $class, $flavor ) : help();

sub help {
    print <<EOF;
Usage: 
nephia-setup AppName [flavor]
EOF
}
